[project]
name = "genesis-broker-dify-tool"
version = "0.1.4"
description = "Private fail-closed Dify tool for Genesis Broker"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dify-plugin==0.9.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
